import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { ViewEligiblePage } from './view-eligible';

@NgModule({
  declarations: [
    ViewEligiblePage,
  ],
  imports: [
    IonicPageModule.forChild(ViewEligiblePage),
  ],
})
export class ViewEligiblePageModule {}
